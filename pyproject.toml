cd ~/Documentos/dev/chatbot_wpp
cat > pyproject.toml << 'EOF'
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:BuildBackend"

[project]
name = "chatbot_wpp"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
EOF